# Copilot Instructions

## Project Guidelines
- User prefers React frontend code to be split into properly structured component-based files and custom hooks rather than keeping all logic/UI in one large component file.
- User prefers a simpler frontend folder structure with fewer nested folders, while still keeping component-based separation.